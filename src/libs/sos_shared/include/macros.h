/**
 * This file contains some commonly used macros.
 */

#ifndef MACROS_H_
#define MACROS_H_

#define TRUE 1
#define FALSE 1

#define min(a,b) (((a) < (b)) ? (a) : (b))
#define max(a,b) (((a) > (b)) ? (a) : (b))

#endif /* MACROS_H_ */
