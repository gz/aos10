/**
 * This file contains some commonly used macros.
 */

#ifndef MACROS_H_
#define MACROS_H_

#define TRUE 1
#define FALSE 0 // yes, 0 and NOT 1 ... ROFL

#define min(a,b) (((a) < (b)) ? (a) : (b))
#define max(a,b) (((a) > (b)) ? (a) : (b))

#endif /* MACROS_H_ */
